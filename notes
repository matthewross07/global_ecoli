fig 1: one notch smaller still on the hex bins.

fig 2:
 + i think this will work better with the wisconsin map on the left and the three plot panels on the right, as before.
 + the column title for the plot panels can be "E. coli observations by monitoring objective, 2022", which will allow the individual panel labels to convey e.g. "Swimming (daily req.)", as before. no need to clarify beach vs river or show the number of points. and this way we can omit the legend for the map panel.
 + the map panel will be A, and the plots will be B-D. No need for e.g. B1
 + the map panel title can be "Wisconsin, USA monitoring sites". no need for an overall title
 + we can use a shared y axis
 + colors and threshold line aren't labeled. if labeling them creates too much clutter, we can remove that information from this plot
 + if we do label the colors, we should change the color of the site points on the map so that it doesn't collide

